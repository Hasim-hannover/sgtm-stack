# sgtm-stack

Self-hosted Server-side GTM (Docker + Caddy).

## Deploy (Kurz)
1) `config/container-config.json` aus GTM (Manual self-hosted) **NICHT** committen – per SCP auf den Server unter `./config/` kopieren.
2) DNS: `sgtm2.deinedomain.de` → Server-IP
3) Auf dem Server:
